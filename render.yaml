services:
  - type: web
    name: openclaw
    env: docker
    plan: free
    region: frankfurt
    dockerCommand: node openclaw.mjs gateway --allow-unconfigured --mode=local
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: GEMINI_API_KEY
        sync: false
      - key: PORT
        value: 8080
